<Window x:Class="SystemMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Monitor"
        Width="230"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Reusable style for the little horizontal usage bars -->
        <Style x:Key="MeterBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Foreground" Value="#00D26A"/>
            <Setter Property="Background" Value="#2A2E35"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid ClipToBounds="True">
                            <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                            <Border x:Name="PART_Track" Background="Transparent"/>
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="3"
                                    HorizontalAlignment="Left"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B7BEC9"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <Border Background="#E614161C" CornerRadius="12" Padding="14">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>

        <StackPanel>
            <!-- Header / drag handle -->
            <Grid Margin="0,0,0,10">
                <TextBlock Text="SYSTEM MONITOR" FontSize="10" FontWeight="Bold"
                           Foreground="#6C7683" HorizontalAlignment="Left"/>
                <TextBlock x:Name="CloseButton" Text="✕" FontSize="11" Foreground="#6C7683"
                           HorizontalAlignment="Right" Cursor="Hand"
                           MouseLeftButtonDown="CloseButton_MouseLeftButtonDown"/>
            </Grid>

            <!-- CPU -->
            <Grid Margin="0,0,0,3">
                <TextBlock Text="CPU" Style="{StaticResource LabelText}" HorizontalAlignment="Left"/>
                <TextBlock x:Name="CpuValueText" Text="0%" Style="{StaticResource ValueText}"/>
            </Grid>
            <ProgressBar x:Name="CpuBar" Style="{StaticResource MeterBar}" Margin="0,0,0,10"/>

            <!-- RAM -->
            <Grid Margin="0,0,0,3">
                <TextBlock Text="RAM" Style="{StaticResource LabelText}" HorizontalAlignment="Left"/>
                <TextBlock x:Name="RamValueText" Text="0%" Style="{StaticResource ValueText}"/>
            </Grid>
            <ProgressBar x:Name="RamBar" Style="{StaticResource MeterBar}" Foreground="#3B9EFF" Margin="0,0,0,10"/>

            <!-- Disk -->
            <Grid Margin="0,0,0,3">
                <TextBlock Text="DISK" Style="{StaticResource LabelText}" HorizontalAlignment="Left"/>
                <TextBlock x:Name="DiskValueText" Text="0 MB/s" Style="{StaticResource ValueText}"/>
            </Grid>
            <ProgressBar x:Name="DiskBar" Style="{StaticResource MeterBar}" Foreground="#FFA53B" Margin="0,0,0,10"/>

            <!-- Network -->
            <Grid Margin="0,0,0,2">
                <TextBlock Text="↓ DOWN" Style="{StaticResource LabelText}" HorizontalAlignment="Left"/>
                <TextBlock x:Name="NetDownText" Text="0 Kbps" Style="{StaticResource ValueText}"/>
            </Grid>
            <Grid Margin="0,0,0,2">
                <TextBlock Text="↑ UP" Style="{StaticResource LabelText}" HorizontalAlignment="Left"/>
                <TextBlock x:Name="NetUpText" Text="0 Kbps" Style="{StaticResource ValueText}"/>
            </Grid>

            <!-- Mini history graph: CPU (green) vs RAM (blue) over the last ~40s -->
            <Border Background="#14FFFFFF" CornerRadius="4" Margin="0,10,0,0" Padding="0">
                <Canvas x:Name="GraphCanvas" Height="40" ClipToBounds="True">
                    <Polyline x:Name="RamGraphLine" Stroke="#3B9EFF" StrokeThickness="1.5"/>
                    <Polyline x:Name="CpuGraphLine" Stroke="#00D26A" StrokeThickness="1.5"/>
                </Canvas>
            </Border>
        </StackPanel>
    </Border>
</Window>
